<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcomfsadfsdafse!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link href="{{ asset('assets/css/style.css') }}" type="text/css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block header %}
           <div id="header">
                <h1>Tareas Symfony</h1>
                <ul id="menu">
                    {% if app.user %}
                        <li><a href="{{ path('tasks') }}">Inicio</a></li>
                        <li><a href="{{ path('my_tasks') }}">Mis tareas</a></li>
                        <li><a href="{{ path('task_create') }}">Crear tarea</a></li>
                        <li class="right"><a href="{{ path('logout') }}">Cerrar sesión</a></li>
                        <li class="right"><a href="{{ path('tasks') }}">{{ app.user.name ~ '' ~ app.user.surname }}</a></li>  
                    {% else %}
                        <li><a href="{{ path('login') }}">Login</a></li>                    
                        <li><a href="{{ path('registro') }}">Registro</a></li>     
                    {% endif %}

                </ul>     
           </div>
        {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>
